//same method name with variable signature called as over load . 
//same method with different signature is called over ride
public virtual class polymophisym_eg1_overload_p {
    public  virtual void mahesh(integer g) {
        System.debug('parent');
    }

}